{
  "name": "@runescape-ai/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "osrs": "bin/osrs.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx test/*.test.ts",
    "osrs": "tsx src/cli.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.8",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
